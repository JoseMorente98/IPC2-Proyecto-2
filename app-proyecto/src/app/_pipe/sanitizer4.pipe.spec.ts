import { Sanitizer4Pipe } from './sanitizer4.pipe';

describe('Sanitizer4Pipe', () => {
  it('create an instance', () => {
    const pipe = new Sanitizer4Pipe();
    expect(pipe).toBeTruthy();
  });
});
