import { Sanitizer3Pipe } from './sanitizer3.pipe';

describe('Sanitizer3Pipe', () => {
  it('create an instance', () => {
    const pipe = new Sanitizer3Pipe();
    expect(pipe).toBeTruthy();
  });
});
