import { Sanitizer2Pipe } from './sanitizer2.pipe';

describe('Sanitizer2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Sanitizer2Pipe();
    expect(pipe).toBeTruthy();
  });
});
